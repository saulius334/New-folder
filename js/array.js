// kompleksinis duomenu tipas AKA masyvas AKA sarasas (kartais matrica)



/*    const p1 = 10;
    const p2 = 2;
    const p3 = 9;
    const p4 = 3;
    const p5 = 1;


    const vidurkis = (p1 + p2 + p3 + p4 + p5) / 5;
    console.log(vidurkis); 
    
    
    

    const marks = [10, 2, 9, 3, 1];
    console.log(marks);

    const suma = 10 + 2 + 9 + 3 + 1;
    const kiekis = 5;

    const vidurkis = suma / kiekis;
    console.log(`vidurkis:`,vidurkis);


*/

    const students = [`saulius`, `paulius`, `aulius`]
    const marks = [10, 2, 9, 3, 1];
    console.log(marks);

    const suma = marks[0] + marks[1] + marks[2] + marks[3] + marks[4];
    const kiekis = marks.length;

    const vidurkis = suma / kiekis;
    console.log(`vidurkis:`,vidurkis);

    const random = [7, 777, `saulius`, 9, true, `labs`, false]; //taip nerasyti






